const styles = theme => ({
  root: {
    padding: theme.spacing.unit * 2
  },
  paper: {
    margin: "0 auto",
    padding: theme.spacing.unit * 2
  },
  textField: {
    fontSize: "16px",
    marginTop: theme.spacing.unit * 2
  }
});

export default styles;
